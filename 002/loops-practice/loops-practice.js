console.log('hello loops-practice!');
function setup() {
    var canvas = createCanvas(500, 500);
    canvas.parent("p5canvas");
}

function draw() {
    
    // from starting at 0; go up to 10; incrementing 1 each time
    // - draw a circle at 

    // from starting at 2; go up to 20; incrementing 2 each time

    // from starting at 2; go up to 20; incrementing 2 each time

    // from starting at 10; go down to 0; decremeting 1 each time

    background(100);
    ellipse(200, 200, 100, 100);
}
